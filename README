This project contains small demo OpenCL programs.

The intention is to provide simple C programs that
can drive KLEE-CL (compile with whole-llvm).

Here are the basic install steps (for non klee-cl use)

$ mkdir simple-opencl-programs
$ cd simple-opencl-programs
$ git clone git@github.com:delcypher/simple-opencl-programs.git src
$ cd src
$ git submodule init
$ git submodule update
$ cd ../
$ mkdir bin
$ cd bin
$ cmake ../src # Feel free to use cmake-gui or ccmake instead
$ make
